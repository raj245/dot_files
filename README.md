This is my dot filtes including vim, tmux, and hg configuration files


Installation
============

Grab the plugin submodules:
'git submodule init && git submodule update`

Update all submodules:
`git submodule foreach git pull origin master`
